<Window x:Class="SLAMRewrite.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SLAMRewrite"
        mc:Ignorable="d"
        Title="SLAMRewrite"
        Height="600"
        Width="1200"
        ResizeMode="CanResizeWithGrip">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition></RowDefinition>
            <RowDefinition></RowDefinition>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition></ColumnDefinition>
            <ColumnDefinition></ColumnDefinition>
        </Grid.ColumnDefinitions>
        <StackPanel
            Name="AllButtonsStackPanel"
            Grid.Row="0"
            Grid.Column="1"
            VerticalAlignment="Center"
            HorizontalAlignment="Center">
            <WrapPanel
                Name="WrapPanelForPlayPauseStop">
                <Button
                    Name="PlayButton"
                    Content="Play/Resume"
                    Width="100"
                    Height="50"
                    Click="PlayButton_OnClick" />
                <Button
                    Name="PauseButton"
                    Content="Pause"
                    Width="100"
                    Height="50"
                    Click="PauseButton_OnClick" />
                <Button
                    Name="StopFlushButton"
                    Content="Stop/Flush"
                    Width="100"
                    Height="50"
                    Click="StopFlushButton_OnClick" />
            </WrapPanel>
        </StackPanel>
        <TextBlock
            Name="StatusTextBlock"
            Grid.Row="0"
            Grid.Column="0"
            HorizontalAlignment="Center"
            VerticalAlignment="Center"
            FontSize="16"
            TextWrapping="Wrap">
            <TextBlock.Text>
                <MultiBinding StringFormat="{}Status: {0}&#x0a;Now Playing: {1}">
                    <Binding Path="SongStatus" />
                    <Binding Path="NowPlaying" />
                </MultiBinding>
            </TextBlock.Text>
        </TextBlock>
        <ListView
            Name="AudioTracksListView"
            Grid.Row="1"
            Grid.Column="0"
            Grid.ColumnSpan="2"
            SelectionMode="Single"
            SelectionChanged="AudioTracks_OnSelectionChanged">
            <ListView.ContextMenu>
                <ContextMenu>
                    <MenuItem Header="Add Song" Click="AddSong_OnClick" />
                    <MenuItem Header="Delete Song" Click="DeleteSong_OnClick" />
                </ContextMenu>
            </ListView.ContextMenu>
        </ListView>
    </Grid>
</Window>