A (minimal) re-write of SLAM using modern .NET!

* Does not require admin privileges to run
* Audio volume can be controlled via volume mixer, though defaults are set
* Separate streams for playback in-game vs locally
* Does not require any "exec slam" or similar commands to be run in the console